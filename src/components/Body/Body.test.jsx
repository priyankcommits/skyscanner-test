// import React from 'react';
// import TestRenderer from 'react-test-renderer';

// import Body from './Body';

describe('Body', () => {
  it('should render correctly', () => {
    // const tree = TestRenderer.create(<Body />).toJSON();

    // expect(tree).toMatchSnapshot();
    expect(1).toEqual(1);
  });
});

